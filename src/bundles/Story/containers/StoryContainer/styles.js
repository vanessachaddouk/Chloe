/* @flow */

import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  swiper: {
  },
  button: {
    position: 'absolute',
    zIndex: 10,
  },
  nextButton: {
    paddingLeft: 2,
  },
  prevButton: {
    paddingRight: 2,
  },
})

export default styles
